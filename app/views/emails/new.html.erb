<h1>New email</h1>
<%= simple_form_for([@email]) do |f| %>
  <%= f.input :rating, input_html: { min: 1, max: 5, step: 1}, label: 'Rate 1 - 5', error: 'needs to be 1 to 5' %>
  <%= f.input :content %>
  <%= f.submit "Submit Review", class: "btn btn-primary" %>
<% end %>
